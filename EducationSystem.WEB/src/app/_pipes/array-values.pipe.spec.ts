import { ArrayValuesPipe } from './array-values.pipe';

describe('ArrayValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
